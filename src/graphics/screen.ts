import { LCD_SIZE_X, LCD_SIZE_Y, FrameBuffer } from './frameBuffer';

export class Screen {
    update(frameBuffer: FrameBuffer) {
        // for(let i = 0; i < MAP_SIZE_Y; i++) {
        //     for(let j = 0; j < MAP_SIZE_X; j++) {
        //         if (frameBuffer[i * j] === 0) {
        //             process.stdout.write(' ');
        //         } else {
        //             process.stdout.write('X');
        //         }
        //     }
        //     process.stdout.write('\n');
        // }

    }
}
