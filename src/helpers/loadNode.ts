import { MemoryMapper } from "../memorymapper";

/**
* Should load a rom to MMU
*/
export const loadRom = (mm: MemoryMapper, romname: string = "rom.rom") => {
    console.log("not implemented yet");
}
